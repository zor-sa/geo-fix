---
status: planned
depends_on: [1, 2, 3]
wave: 3
skills: [test-master]
verify: []
reviewers: []
teammate_name:
---

# Task 6: Test Audit

## Required Skills

Before starting, load: `/skill:test-master` — [SKILL.md](~/.claude/skills/test-master/SKILL.md)

## Description

Full-feature test quality audit across all test files created in this feature. Tasks 1–3 each write tests per TDD anchors in their task files. This audit verifies the complete test suite holistically: coverage of all acceptance criteria, meaningful assertions (not just "no exception"), test pyramid balance, and that integration tests use real mitmproxy (not mocked) per architecture.md.

Known test files from task specs:
- `test/test_system_config.py` — registry functions, cleanup label, ProxyState backward compat (Task 1)
- `test/test_proxy_addon.py` — geolocation API intercept, non-target injection, CSP skip guard, permissions override (Task 2)
- Any additional test files created by Tasks 1–3

Check all test files for this feature, including any new ones created during implementation.

## What to do

1. List all test files in `test/` to identify what was created or modified by Tasks 1–3
2. Read all feature-related test files
3. Map each acceptance criterion from user-spec and tech-spec to at least one test — identify gaps
4. Review test quality: meaningful assertions, not just "passes without exception", realistic mock/stub setups, correct use of `monkeypatch` for registry isolation
5. Verify test pyramid balance: unit tests cover edge cases, integration tests use real mitmproxy (no mitmproxy mocking at integration level per architecture.md)
6. Check that Windows-specific tests are skipped on Linux via `skipif` (matching existing pattern for 13 Windows-specific tests)
7. Write structured test audit report to `logs/working/task-6/test-audit.json`

## Acceptance Criteria

- [ ] All test files created/modified in this feature identified and read
- [ ] Each tech-spec acceptance criterion mapped to at least one test (gaps documented)
- [ ] Test quality reviewed: meaningful assertions, realistic setups, correct isolation
- [ ] Test pyramid balance verified: unit vs integration split, real mitmproxy in integration tests
- [ ] Windows-specific tests properly guarded with `skipif`
- [ ] Audit report written to `logs/working/task-6/test-audit.json` with findings and severities

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)
- [project.md](/home/claude/workspace/projects/geo-fix/.claude/skills/project-knowledge/references/project.md)
- [architecture.md](/home/claude/workspace/projects/geo-fix/.claude/skills/project-knowledge/references/architecture.md)
- [patterns.md](/home/claude/workspace/projects/geo-fix/.claude/skills/project-knowledge/references/patterns.md)
- [test/test_system_config.py](/home/claude/workspace/projects/geo-fix/test/test_system_config.py)
- [test/test_proxy_addon.py](/home/claude/workspace/projects/geo-fix/test/test_proxy_addon.py)

## Verification Steps

### Smoke

- Audit report exists and is valid JSON: `python -c "import json; json.load(open('logs/working/task-6/test-audit.json')); print('OK')"`

## Details

**Required test coverage per tech-spec Testing Strategy section:**

Unit tests that must exist:
- `disable_location_services()` / `restore_location_services()` — mock winreg, verify read-before-write, verify original value preservation, verify `None` original → key deletion on restore
- `restore_location_services()` rejects invalid original values (not in `{'Allow', 'Deny', None}`) → defaults to `Deny`
- Non-Windows platform: both functions are no-ops
- `cleanup()` with new Location Services label — retry and pending file behavior
- ProxyState backward compat: old state file without `original_location_services` deserializes with default `None`
- Geolocation API intercept in `request()` — exact host/path/method match → synthetic response with randomized accuracy
- Geolocation API intercept skips non-matching URLs, non-POST methods
- Geolocation API intercept error path — mock to raise → log, pass through, no re-raise
- `navigator.permissions.query` override scoped to `name === 'geolocation'` only
- Geolocation-only payload on non-target domain vs full payload on target domain
- CSP skip: non-target domain with `script-src 'none'` → no injection

Integration tests that must exist:
- Proxy flow: non-target domain HTML → response contains geolocation override JS (but not timezone/language)
- Proxy flow: POST to googleapis.com/geolocation → returns fake JSON coordinates
- Proxy flow: target domain HTML → response contains full JS payload (regression)

**Quality checks per test:**
- Registry tests: `monkeypatch` used for winreg isolation (not real registry writes in unit tests)
- Integration tests: real mitmproxy `Master` instance on free port (no mitmproxy mocking — per architecture.md "Real mitmproxy in integration tests")
- Accuracy randomization test: verify value is in range 40–80m, not just that it exists
- Error path tests: verify exception is logged AND request passes through (two assertions, not one)
- Permissions override: verify that `query({name: 'camera'})` still returns original result (not just that geolocation returns 'granted')

**Existing test pattern to match:**
- Windows-specific tests: `@pytest.mark.skipif(sys.platform != 'win32', reason='Windows only')`
- Integration test setup: existing pattern in `test/test_integration_proxy.py` (real Master on free port)

**Report format:** JSON with fields `task` (6), `coverage_gaps` (array of missing tests), `quality_findings` (array of `{severity, file, test, issue}`), `summary` (string)

## Reviewers

No reviewers for audit tasks.

## Post-completion

- [ ] Write brief report to decisions.md (Summary: 1-3 sentences, link to test audit JSON, no findings tables)
- [ ] If deviated from spec — describe deviation and reason
- [ ] Update user-spec/tech-spec if anything changed
